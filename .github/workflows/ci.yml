name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.27.1'
          cache: false
      - run: go mod download
      - run: go mod tidy && git diff --exit-code -- go.mod go.sum
      - run: test -z "$(find ./cmd ./internal ./web -name '*.go' -print0 | xargs -0 gofmt -l)"
      - run: go vet ./...
      - run: go test ./...
      - run: go build ./cmd/x-type-center ./cmd/type-registry
      - run: ./x-type-center version
